using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AppLancher : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("the new start!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
